# CBM Shaft Anomaly Detection — consolidated requirements

# Data processing
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyarrow>=10.0        # parquet support

# Machine learning
scikit-learn>=1.2
# TF 2.20+ deadlocks (absl mutex) during keras.fit when mlflow/grpc is imported
# in the same process (observed on macOS arm64, TF 2.21 + mlflow 2.x/3.x)
tensorflow>=2.16,<2.20   # Autoencoder / LSTM-AE (training & evaluation only)

# Experiment tracking
mlflow>=2.9

# Dashboard
streamlit>=1.40
plotly>=5.24

# Visualization
matplotlib>=3.6
seaborn>=0.12

# Utilities
tqdm>=4.64
joblib>=1.2
